#!/bin/bash
# wallet-stats — Spending analytics from the transaction log
# Shows totals, daily breakdown, and top contracts called
curl -sf http://127.0.0.1:8128/wallet-stats 2>/dev/null || echo '{"error":"signing proxy not running"}'
