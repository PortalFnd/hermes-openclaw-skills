#!/bin/bash
# wallet-summary — Full wallet overview: balances, spending, approvals, recent activity
curl -sf http://127.0.0.1:8128/wallet-summary 2>/dev/null || echo '{"error":"signing proxy not running"}'
